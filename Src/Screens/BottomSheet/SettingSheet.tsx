import {StyleSheet, Text, View} from 'react-native';
import React, {FC} from 'react';

const SettingSheet: FC = () => {
  return <View></View>;
};

export default SettingSheet;

const styles = StyleSheet.create({});
